create table actor_orc(movie_id int,actor_id int,actor_name string,isleading string)
row format delimited fields terminated by ','
stored as orc
tblproperties("orc.compress"="NONE");

insert into table actor_orc select * from actor;
