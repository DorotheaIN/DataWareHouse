create table actor(movie_id int,actor_id int,actor_name string,isleading string)
row format delimited fields terminated by ',';

load data local inpath '/opt/module/data/actor.txt' into table actor;

